[project]
name = "fork-of-text-simplification-datasets"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["pyyaml", "tabulate", "ruff", "ty"]
